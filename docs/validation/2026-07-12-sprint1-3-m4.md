# Sprint 1-3 and M4 validation record - 2026-07-12

Validated source baseline: `40c754ee` (`master`, matching `origin/master` at
the start of validation).

Local GPU host: `MOONSYS`, Windows `10.0.22631`, NVIDIA GeForce RTX 4070 SUPER,
driver `32.0.15.9579`. Virtual display adapters were present, so this record is
not substituted for isolated self-hosted-runner evidence.

## Local gates

| Gate | Result | Evidence |
| --- | --- | --- |
| Debug smoke | PASS | `tools/smoke.ps1`; debug build, RHI boundary check, and all 162 engine/editor tests passed. |
| Release package smoke | PASS | `tools/release-smoke.ps1 -SoakSeconds 60 -ReloadIterations 10`. |
| D3D11 package launch | PASS | Published Player launched with scene override. |
| D3D12 package launch | PASS | Published Player launched successfully. |
| Device-loss contract | PASS | Both backends returned exit code 3 with validated diagnostic reports. |
| Reload gate | PASS | Ten isolated D3D11 scene-load processes passed. |
| Failure paths | PASS | Corrupt archive, missing scene/runtime/transitive DLL, tampered dependency manifest, and invalid project config were rejected. |

The initial release-smoke attempt encountered transient Windows sharing status
`0xC0000043` while the IconTool regenerated `editor.ico` after switching the
checkout from debug to release. A single bounded retry was added to the
idempotent IconTool target. The complete release gate then passed.

The local gate used the CI defaults: a 60-second launch on each stable backend
and ten isolated reload processes. It proves the functional and working-set
gate on the current RTX 4070 SUPER host, but does not replace the required
self-hosted workflow run record.

## Accepted milestone exceptions

- Retain the first green hosted `Windows CI` run URL for `master`.
- Configure `myengine-gpu-production` with a `master` branch restriction and a
  required reviewer, then prove an untrusted ref is rejected.
- Retain the first self-hosted GPU run URL, commit, runner image, GPU/driver
  versions, backend results, resource-growth result, and archived artifacts.
- Execute real driver/device removal in the GPU fault-injection lab; deterministic
  engine injection is already covered but is not evidence of a real driver reset.
  `tools/gpu-device-loss-lab.ps1` now provides the supervised harness and rejects
  ordinary/synthetic exits; an actual isolated-host fault is still required.
- Decide explicitly whether rewriting published `master` is acceptable. Commit
  `40c754ee` combines the Sprint 1-3/M4 implementation and therefore does not
  satisfy the roadmap's reviewable-commit-history item. Rewriting a shared
  protected branch is not performed automatically.

At 2026-07-12 15:36 +08:00, the project owner explicitly accepted all items in
this section as exceptions for the current Sprint 1-3/M4 milestone. Their status
is `N/A (accepted exception)`, not PASS. They remain useful future hardening
work, but no longer block closure of this milestone. No hosted/self-hosted run,
real device removal, branch-history rewrite, or substitute evidence is claimed.

## Current worktree regression - 2026-07-12 15:31 +08:00

The local gates were rerun after the uncommitted M5 performance and task-service
changes so the earlier Sprint 1-3/M4 evidence would not be inferred from stale
binaries.

| Gate | Result | Current-worktree evidence |
| --- | --- | --- |
| Debug smoke | PASS | `tools/smoke.ps1`; RHI boundary check, full debug build, and 165/165 tests passed. A transient busy `SDL3.dll` copy was recovered by the single bounded retry. |
| Release package smoke | PASS | `tools/release-smoke.ps1 -SoakSeconds 60 -ReloadIterations 10`; 56 cooked files, package validation, and performance report gates passed. |
| Stable backend launch | PASS | Published D3D11 and D3D12 Players completed the 60-second local gate. |
| Device-loss contract | PASS | Both deterministic backend injections exited with code 3 and validated diagnostic reports. |
| Reload/failure paths | PASS | Ten isolated D3D11 reloads and corrupt archive, missing scene/runtime/transitive DLL, tampered dependency manifest, and invalid project config rejection passed. |

This refresh validates the current local worktree only. Together with the
explicit exception decision above, Sprint 1-3/M4 is `READY WITH ACCEPTED
EXCEPTIONS` for the current milestone.
