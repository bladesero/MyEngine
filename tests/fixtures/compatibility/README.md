# Compatibility fixtures

These files are immutable published-format inputs. Tests may add newer fixtures,
but must not rewrite an existing fixture to match current output. A format change
must either migrate the corresponding fixture or reject it explicitly.
